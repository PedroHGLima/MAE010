name: Compile and Zip

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends make openmpi-bin libopenmpi-dev

      - name: Compile available Makefiles
        shell: bash
        run: |
          shopt -s nullglob
          for makefile in ex*/Makefile; do
            dir="$(dirname "$makefile")"
            echo "Compilando $dir"
            make -C "$dir"
          done

  package:
    needs: build
    runs-on: ubuntu-latest
    container:
      image: texlive/texlive:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install zip
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends zip

      - name: Generate zip
        shell: bash
        run: |
          bash ./pack.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: T1_Lima_Pedro
          path: T1_Lima_Pedro.zip
